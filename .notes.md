## Running Assembled Application
`C:\Users\andre\.jdks\corretto-17.0.12\bin\java.exe -jar build\libs\Processing-1.0-SNAPSHOT.jar`

## Misc.
```
public static float toVertical(float fov, float inverseAspect) {
    return 2 * (float) atan(tan(fov / 2) * inverseAspect);
}

//        final int halfLength = length / 2;
//        final int index = cell + halfLength;
//        return ((-(index / halfLength) >> 31) & ((length << 31) >> 31)) + index;
```